ALTER TABLE site_settings
  ADD COLUMN IF NOT EXISTS site_logo TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS favicon TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS contact_email TEXT NOT NULL DEFAULT 'info@techpinik.com',
  ADD COLUMN IF NOT EXISTS contact_phone TEXT NOT NULL DEFAULT '+8801814931931',
  ADD COLUMN IF NOT EXISTS contact_address TEXT NOT NULL DEFAULT 'Dhaka, Bangladesh',
  ADD COLUMN IF NOT EXISTS support_email TEXT NOT NULL DEFAULT 'support@techpinik.com',
  ADD COLUMN IF NOT EXISTS currency TEXT NOT NULL DEFAULT 'BDT',
  ADD COLUMN IF NOT EXISTS currency_symbol TEXT NOT NULL DEFAULT '৳',
  ADD COLUMN IF NOT EXISTS timezone TEXT NOT NULL DEFAULT 'Asia/Dhaka',
  ADD COLUMN IF NOT EXISTS date_format TEXT NOT NULL DEFAULT 'DD/MM/YYYY',
  ADD COLUMN IF NOT EXISTS min_order_amount INTEGER NOT NULL DEFAULT 500,
  ADD COLUMN IF NOT EXISTS free_shipping_threshold INTEGER NOT NULL DEFAULT 1000,
  ADD COLUMN IF NOT EXISTS default_shipping_cost INTEGER NOT NULL DEFAULT 60,
  ADD COLUMN IF NOT EXISTS order_prefix TEXT NOT NULL DEFAULT 'TP',
  ADD COLUMN IF NOT EXISTS facebook_url TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS instagram_url TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS twitter_url TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS youtube_url TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS enable_reviews BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS enable_wishlist BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS enable_compare BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS enable_newsletter BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS google_analytics_id TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS maintenance_mode BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS maintenance_message TEXT NOT NULL DEFAULT 'We are currently performing scheduled maintenance. Please check back soon.';
